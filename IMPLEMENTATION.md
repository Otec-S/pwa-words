# Реализованный функционал PWA "Слова"

## ✅ Полностью реализовано

### Структура карточек
- ✅ 3 карточки с уникальным контентом
- ✅ 4 визуально разделенные группы на каждой карточке:
  - Группа 1: 3 слова × 3 очка = 9 очков
  - Группа 2: 3 слова × 3 очка = 9 очков
  - Группа 3: 1 слово × 5 очков = 5 очков
  - Группа 4: 1 слово/фраза × 7 очков = 7 очков
- ✅ Максимум 30 очков на карточку

### Интерактивность
- ✅ Чекбоксы слева от каждого слова
- ✅ Подсчет очков при клике на чекбокс
- ✅ Итоговое число очков внизу карточки (изначально 0)

### Таймер
- ✅ Обратный отсчет 2:00 минуты
- ✅ Кнопка СТАРТ/СТОП в верхней части карточки
- ✅ Автоматический сброс при переключении карточек
- ✅ Звуковой сигнал при завершении (Web Audio API, 900Hz, 700ms)
- ✅ Красный фон карточки при истечении времени

### Навигация
- ✅ Кнопки ВПЕРЕД и НАЗАД под карточками
- ✅ Свайп-жесты влево/вправо
- ✅ Поддержка клавиш ← и → на клавиатуре
- ✅ CSS-анимации перелистывания (slideIn/Out, 300ms)

### PWA функциональность
- ✅ Манифест с именем "PWA слова"
- ✅ Service Worker для офлайн работы
- ✅ SVG иконки 192×192 и 512×512
- ✅ Standalone режим отображения
- ✅ Автоматическое обновление (registerType: 'autoUpdate')

### Технические особенности
- ✅ React 19.2 + TypeScript 5.9
- ✅ Vite 6 как сборщик
- ✅ Responsive дизайн (mobile-first)
- ✅ Safe area insets для iOS
- ✅ user-scalable=no для предотвращения зума
- ✅ БЕЗ сохранения прогресса (по требованию)

## 📦 Структура файлов

```
pwa-words/
├── src/
│   ├── components/
│   │   ├── Card/
│   │   │   ├── Card.tsx         - Главный компонент карточки
│   │   │   └── Card.css         - Стили + анимации
│   │   ├── Timer/
│   │   │   ├── Timer.tsx        - Таймер с Web Audio API
│   │   │   └── Timer.css        - Стили таймера
│   │   ├── WordGroup/
│   │   │   ├── WordGroup.tsx    - Группа слов с чекбоксами
│   │   │   └── WordGroup.css    - Стили групп
│   │   └── Navigation/
│   │       ├── Navigation.tsx   - Кнопки навигации
│   │       └── Navigation.css   - Стили навигации
│   ├── hooks/
│   │   ├── useSwipe.ts          - Touch-жесты
│   │   └── useKeyboard.ts       - Клавиатурная навигация
│   ├── types/
│   │   └── index.ts             - TypeScript интерфейсы
│   ├── data/
│   │   └── cards.json           - 3 карточки с данными
│   ├── App.tsx                  - Главный компонент с логикой
│   ├── App.css                  - Стили приложения
│   └── index.css                - Глобальные стили
├── public/
│   └── icons/
│       ├── icon-192x192.svg     - PWA иконка
│       └── icon-512x512.svg     - PWA иконка
├── vite.config.ts               - Конфигурация Vite + PWA
├── index.html                   - HTML с viewport настройками
└── package.json                 - Зависимости

Всего создано: 20+ файлов
```

## 🚀 Команды для запуска

```bash
# Разработка
npm run dev           # http://localhost:5173/

# Production сборка
npm run build         # → dist/

# Просмотр production
npm run preview       # http://localhost:4173/
```

## 🎯 Особенности реализации

### 1. Таймер с Web Audio API
```typescript
const oscillator = audioContext.createOscillator();
oscillator.frequency.value = 900; // Hz
oscillator.start(audioContext.currentTime);
oscillator.stop(audioContext.currentTime + 0.7); // 700ms
```

### 2. Автосброс при смене карточки
```typescript
useEffect(() => {
  setTimeLeft(timerDuration);
  setIsRunning(false);
}, [cardId]); // Зависимость от ID карточки
```

### 3. Анимации перелистывания
```css
@keyframes slideInRight {
  from { transform: translateX(100%); }
  to { transform: translateX(0); }
}
```

### 4. Свайпы
```typescript
const distance = touchStartX - touchEndX;
if (distance > 50) onSwipeLeft(); // Порог 50px
```

## ✨ Дополнительные фичи

- ✅ Визуальное разделение групп (border, padding)
- ✅ Отображение очков справа от каждой группы
- ✅ Disabled состояние для кнопок навигации на границах
- ✅ Градиентный фон приложения
- ✅ Адаптивная типографика
- ✅ Accessibility: aria-labels, keyboard support

## 📱 Тестирование PWA

1. Запустить `npm run build && npm run preview`
2. Открыть http://localhost:4173/
3. В DevTools → Application → Manifest проверить настройки
4. В DevTools → Application → Service Workers проверить SW
5. Установить через меню браузера "Установить приложение"

## 🎨 Примеры данных

**Карточка 1 - Базовые действия:**
- run, walk, jump (3 очка)
- read, write, speak (3 очка)
- understand (5 очков)
- communicate effectively (7 очков)

**Карточка 2 - Природа:**
- tree, flower, grass / cat, dog, bird
- ecosystem / biodiversity conservation

**Карточка 3 - Дом и семья:**
- door, window, wall / table, chair, bed
- comfortable / family gathering place

Все требования реализованы! ✅
